#version 460

layout (set = 0, binding = 0, rgba32f) uniform image2D ImageToClear;

layout (local_size_x = 8, local_size_y = 8, local_size_z = 1) in;

void main()
{
    ivec2 UV = ivec2(gl_GlobalInvocationID.xy);

    imageStore(ImageToClear, UV, vec4(0.f));
}