#version 460#extension GL_EXT_ray_tracing : require#extension GL_EXT_nonuniform_qualifier : enablestruct HitPayload{    vec3 HitValue;};layout(location = 0) rayPayloadInEXT HitPayload Hit;hitAttributeEXT vec2 Attribs;void main(){//    Hit.HitValue = vec3(1.0 - Attribs.x - Attribs.y, Attribs.x, Attribs.y);//    return;    vec3 FinalColor = vec3(0.0f, 1.0f, 0.0f);    if ((gl_InstanceCustomIndexEXT % 2) == 0)    {        FinalColor = vec3(1.0f, 0.0f, 0.0f);    }    Hit.HitValue = FinalColor;    return;}